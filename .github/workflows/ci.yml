name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Restore dependencies
        run: dotnet restore  tests/Dameng.Protobuf.Extension.Tests/

      - name: Build solution
        run: dotnet build --configuration Release --no-restore tests/Dameng.Protobuf.Extension.Tests/

      - name: Run tests
        run: dotnet test --configuration Release --no-build --verbosity normal tests/Dameng.Protobuf.Extension.Tests/

      - name: Test package creation
        run: |
          dotnet pack --configuration Release --no-build \
            --output ./test-packages src/Dameng.Protobuf.Extension/
