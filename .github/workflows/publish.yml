name: Publish NuGet Packages

on:
  push:
    tags:
      - 'v*.*.*'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 1.0.0)'
        required: true
        default: '1.0.0'

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Restore dependencies
        run: dotnet restore  tests/Dameng.Protobuf.Extension.Tests/

      - name: Build solution
        run: dotnet build --configuration Release --no-restore tests/Dameng.Protobuf.Extension.Tests/

      - name: Run tests
        run: dotnet test --configuration Release --no-build --verbosity normal tests/Dameng.Protobuf.Extension.Tests/

      - name: Pack packages
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            VERSION="${{ github.event.inputs.version }}"
          elif [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VERSION="${{ github.ref_name }}"
            VERSION="${VERSION#v}"
          else
            VERSION="not-vaild-version"
          fi

          echo "Publishing version: $VERSION"

          dotnet build src/Dameng.Protobuf.Extension.Generator/Dameng.Protobuf.Extension.Generator.csproj \
            --configuration Release \
            --no-build \
            --output ./packages \
            -p:Version=$VERSION

          dotnet pack src/Dameng.Protobuf.Extension/Dameng.Protobuf.Extension.csproj \
            --configuration Release \
            --no-build \
            --output ./packages \
            -p:Version=$VERSION

      - name: Publish to NuGet
        run: |
          dotnet nuget push ./packages/*.nupkg \
            --api-key ${{ secrets.NUGET_API_KEY }} \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}

      - name: Upload packages as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nuget-packages
          path: ./packages/*.nupkg
